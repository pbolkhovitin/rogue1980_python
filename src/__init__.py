"""Wrapper package to expose the project code under a 'src' namespace for tests.
This module is intentionally lightweight and only exists to satisfy imports
like 'src.domain...'. The actual implementation lives under the top-level
'domain' package.
"""
